<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="kr.co.weple.meet.model.dao.MeetDao">
	<select id="totalCount" resultType="int">
		select count(*) from meet
		where meet_type =1
	</select>
	<select id="selectBoardList" resultType="meet">
		SELECT * FROM
		(SELECT ROWNUM AS RNUM, M.* FROM
		(SELECT
		MEET_NO,
		MEET_TITLE,
		MEET_CONTENT_S,
		MEET_CATEGORY,
		MEET_THUMBNAIL
		FROM MEET
		WHERE MEET_TYPE = 1
		ORDER BY 1 DESC)M)
		WHERE RNUM BETWEEN #{start} AND #{end}
	</select>
	<select id="enrollMemberList" resultType="int">
		select count(*) from
		follower where follower_status=0
	</select>
	
	<!-- 모임생성 -->
	<insert id="createMeet">
		MEET_SEQ.NEXTVAL,
		'2023-10-06',
		'모임타이틀',
		'내용 S',
		'내용 D',
		21, --정원
		20, --남은 인원
		'임시준비물',
		3, --카테고리
		'주소1', --주소
		'주소2', --상세주소
		36, --위도
		126, --경도
		'썸네일', --썸네일
		0, --모임승인 검수중
		'test99' --모임장
	</insert>
</mapper>
